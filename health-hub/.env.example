# =============================================================================
# Sobhana Health Hub — Frontend Environment Configuration
# =============================================================================
#
# PRODUCTION  -> points to Render prod service
# DEVELOPMENT -> points to Render dev service (or local backend)
# =============================================================================

# ===== API =====
# Backend URL (no trailing slash). Defaults to http://localhost:3000 if unset.
#
# PROD:
#   VITE_API_BASE_URL=https://reports.sobhanaportal.com
#
# DEV (your Render dev service URL):
#   VITE_API_BASE_URL=https://sobhana-portal-dev.onrender.com
#
# LOCAL:
#   VITE_API_BASE_URL=http://localhost:3000
VITE_API_BASE_URL=
